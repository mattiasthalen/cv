# Allow inline HTML (RenderCV generates HTML in markdown)
MD033: false

# Allow duplicate headings (CV sections may repeat patterns)
MD024: false

# Allow trailing punctuation in headings
MD026: false

# Don't enforce line length (generated content)
MD013: false

# Allow multiple consecutive blank lines (RenderCV output)
MD012: false

# Allow headings without surrounding blank lines (RenderCV output)
MD022: false

# Allow multiple top-level headings (CV has multiple sections)
MD025: false
